import "package:flutter/material.dart";

class UploadsTab extends StatelessWidget {
  const UploadsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Yuklemeler (MVP)")
    );
  }
}
